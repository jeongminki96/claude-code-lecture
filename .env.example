# 이 파일을 복사해 .env 로 저장하고 값을 채우세요. (.env 는 gitignore 로 커밋되지 않습니다)
# 로컬에서는 server.js / api 함수가 이 값을 읽고, Vercel 에서는 대시보드 환경변수를 사용합니다.

OPENAI_API_KEY=sk-여기에-본인-키
# 선택: 모델 변경 (기본 gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini
